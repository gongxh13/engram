{
  "name": "engram",
  "version": "1.0.0",
  "private": true,
  "description": "Agent Task Evaluation Data Collection System",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "clean": "pnpm -r run clean || true"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.3.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
